export const OTP_REGEX = /[0-9]{6}$/;

// The interval in milliseconds to check for heartbeats from the client
export const HEARTBEAT_INTERVAL = 3 * 60 * 1000;

export const USERNAME_REGEX = /^([a-zA-Z0-9_]{2,24})$/;
